"use client";import{useEffect,useRef}from"react";const vertexShader=`attribute vec2 position;void main(){gl_Position=vec4(position,0.0,1.0);}`,fragmentShader=`precision highp float;uniform float u_time;uniform vec2 u_resolution;uniform float u_grainIntensity;uniform float u_vignetteIntensity;uniform vec3 u_tintColor;uniform float u_tintIntensity;float random(vec2 st){return fract(sin(dot(st.xy,vec2(12.9898,78.233)))*43758.5453123);}void main(){vec2 st=gl_FragCoord.xy/u_resolution.xy;float grain=random(st+u_time*0.01)*u_grainIntensity;vec2 center=st-0.5;float vignette=1.0-dot(center,center)*u_vignetteIntensity;vignette=smoothstep(0.0,1.0,vignette);vec3 tint=u_tintColor*u_tintIntensity*(0.5+0.5*sin(st.x*3.14+u_time*0.2));vec3 color=vec3(grain)*vignette+tint*(1.0-vignette*0.5);float alpha=u_grainIntensity*0.4+u_tintIntensity*0.15;gl_FragColor=vec4(color,alpha);}`;export function WebGLGrain(){const canvasRef=useRef<HTMLCanvasElement>(null),rafRef=useRef<number>(0);useEffect(()=>{const canvas=canvasRef.current;if(!canvas)return;const gl=canvas.getContext("webgl",{premultipliedAlpha:true,alpha:true});if(!gl)return;const compile=(type:number,source:string)=>{const shader=gl.createShader(type)!;gl.shaderSource(shader,source);gl.compileShader(shader);return shader;},program=gl.createProgram()!;gl.attachShader(program,compile(gl.VERTEX_SHADER,vertexShader));gl.attachShader(program,compile(gl.FRAGMENT_SHADER,fragmentShader));gl.linkProgram(program);gl.useProgram(program);const buffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,1,-1,-1,1,1,1]),gl.STATIC_DRAW);const position=gl.getAttribLocation(program,"position");gl.enableVertexAttribArray(position);gl.vertexAttribPointer(position,2,gl.FLOAT,false,0,0);const uTime=gl.getUniformLocation(program,"u_time"),uRes=gl.getUniformLocation(program,"u_resolution"),uGrain=gl.getUniformLocation(program,"u_grainIntensity"),uVignette=gl.getUniformLocation(program,"u_vignetteIntensity"),uTint=gl.getUniformLocation(program,"u_tintColor"),uTintInt=gl.getUniformLocation(program,"u_tintIntensity");gl.uniform1f(uGrain,0.12);gl.uniform1f(uVignette,2.5);gl.uniform3f(uTint,0.078,0.722,0.651);gl.uniform1f(uTintInt,0.08);const resize=()=>{const dpr=Math.min(window.devicePixelRatio,2);canvas.width=canvas.clientWidth*dpr;canvas.height=canvas.clientHeight*dpr;gl.viewport(0,0,canvas.width,canvas.height);gl.uniform2f(uRes,canvas.width,canvas.height);};resize();window.addEventListener("resize",resize);const start=performance.now();const render=()=>{gl.uniform1f(uTime,(performance.now()-start)*0.001);gl.drawArrays(gl.TRIANGLE_STRIP,0,4);rafRef.current=requestAnimationFrame(render);};render();return()=>{cancelAnimationFrame(rafRef.current);window.removeEventListener("resize",resize);gl.deleteProgram(program);gl.deleteBuffer(buffer);};},[]);return<canvas ref={canvasRef} className="absolute inset-0 w-full h-full pointer-events-none z-10" style={{mixBlendMode:"overlay"}}/>;}